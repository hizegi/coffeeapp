<html>
<head>
	<title>TESTING YELP</title>
</head>
<body>

	<h2>Welcome back, <%= user.username %>!</h3>


	<h3> SEARCH HERE BY ZIPCODE</h3>
	<form action="/users/<%= user.id %>/locations/" method="POST">
		<input type="text" name="zipcode" placeholder="zipcode"><br>
		<button>SUBMIT</button>
	</form>

	<h3>User's Locations</h3>
	<ul>
		<% for (var i = 0; i < user.locations.length; i++) { %>
		<li>Location <%= i+1 %>: <a href="/locations/<%= user.locations[i].nameid %>"><%= user.locations[i].name%></a></li>
		<% } %>
	</ul>

	<h3>User's Reviews</h3>
	<ul>
		<% for (var i = 0; i < review.length; i++) { %>
		<li>Favorite: <%= i+1 %>: <%= review[i].best%></li>
		<li>Review: <%= i+1 %>: <%= review[i].comments%></li>
			<% if (usertrue) { %> 
			<a href="/users/<%= user.id %>/review">Edit Review</a>
			<% } %>
		<% } %>

	</ul>

	<a href="/users">Return Home</a>

	<!-- DELETES USER and associated Reviews -->
	<% if (usertrue) { %> 
		<form action="/users/<%= user.id %>" method="POST">
			<input type="hidden" name="_method" value="DELETE">
			<button id="delete-button">Delete Self</button>
		</form>
		<% } %>
</body>
</html>